import { useQuery } from '@tanstack/vue-query'

// export const useMetafileQuery = (metaTxId: string) => {
//   return useQuery({
//     queryKey: ['metafile', { metaTxId }],
//     queryFn: () => fetchMetafile(metaTxId),
//     staleTime: 1000 * 60 * 60 * 24 * 30, // 30 days
//   })
// }
